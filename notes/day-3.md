 What i learned :- 
    - build Student Registration (/student/register, student_register.html)
    - build Student Dashboard (/student, )
    - build /login POST route (redirect("/admin"), redirect("/student"), redirect("/company"))


Error i faced :- 
    1.) i runned the file without saving 

How i fixed it :-

    1.) just save and re-run
